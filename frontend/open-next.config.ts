import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Minimal OpenNext/Cloudflare config. The adapter uses the default
// runtime overrides; add caching/queue bindings here later if we need them.
export default defineCloudflareConfig({});
